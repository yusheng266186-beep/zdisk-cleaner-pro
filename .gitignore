# Rust
/target/
**/*.rs.bk

# Node / 前端
node_modules/
ui/dist/

# Python（v2 遗留构建产物）
__pycache__/
*.py[cod]
build/
dist/

# 杂项
*.log
.DS_Store
Thumbs.db
.zcache/

# 更新签名私钥（绝不入库）
.tauri/
*.tsbuildinfo
latest.json

# 本机 target-dir 重定向(D 盘专属路径,不入库;产物位置以 `cargo metadata` 为准)
.cargo/

# v5 交付物本地暂存区（构建产物副本，不入库）
dist-v5/
